# 教师 AI 工作平台｜独立验收 Gate

> 状态：规划基线
>
> 原则：没有可重复证据就不算完成；静态字符串检查、全部 mock 的测试和“进程能启动”不能代替真实行为验收。

## 一、证据层级

### G1｜代码与规则

- 每个迁移项记录旧仓库提交 SHA、源路径、直接依赖、对应测试、处置类别、目标路径和独立验收结果；
- 无法回指旧来源的能力必须明确标记为用户确认的新增需求；
- 类型、格式、依赖方向和 500 行上限；
- 领域规则单元测试；
- 请求/返回运行时契约；
- 禁止能力静态检查。

MIG-002 过渡规则：固定快照中的超长 legacy 文件只有在主 manifest 记录路径、行数、blob/hash、替换任务和“修改前先拆分”约束时才能暂存；该例外不能用于新增或已修改文件，也不能被表述为 500 行正式 Gate 已通过。

### G2｜数据与模块集成

- 使用真实 PostgreSQL、对象存储替身和持久化任务；
- 迁移、事务、幂等、并发和进程重启；
- 跨租户攻击矩阵；
- 备份、恢复和数据对账。

### G3｜真实用户流程

- 浏览器真实路由、刷新、深链和草稿恢复；
- 日程月份、日期和视图状态可由地址恢复；手机默认日程不依赖页面级横向周表；
- AI 会话栏在桌面可展开收起、在手机以抽屉打开；切换会话不混淆草稿、学生上下文和待确认操作；
- Windows 11 Edge/Chrome 与手机宽度；
- 键盘、焦点、触控和可访问性；
- 教师、管理员、微信三条身份轨独立验证。

### G4｜外部服务

- 真实 OCR、ASR、AI 和最终微信供应商；
- 成功、低可信、失败、超时、限流、重复和恢复；
- 数据地区、保留行为、费用上限与停用路径；
- placeholder 在预发布和正式环境必须 fail-closed。

### G5｜生产同构交付

- 干净 Linux 环境锁文件安装、测试和构建；
- 固定镜像摘要在预发布验证后原样提升；
- 数据迁移、回退、监控、告警和恢复演练；
- 产物清单、SHA-256、依赖清单和安全扫描。

### G6｜独立审查

- 审查者不负责该波主要实现；
- 从产品契约反向检查，不依赖实现者口头说明；
- 高风险问题未关闭时不得通过；
- 用户结果、失败路径和明确禁止能力都有证据。

## 二、全局零容忍项

以下任一出现，候选版本直接失败：

- 教师能读取、修改或导出其他教师数据；
- 管理员绕过已确认的内容访问边界；
- OCR、ASR、AI 或微信占位实现被标记为生产可用；
- 平台存在主动、定时、批量或无入站请求的微信发送路径；
- AI 猜测未经规则或教师确认直接成为学生事实；
- 失败、部分成功或超时被界面显示为成功；
- 重复消息、重复点击或重试造成重复扣课时、重复缴费或重复记录；
- 密钥或真实数据进入代码、日志、测试夹具、镜像或发布包；
- 未经授权执行公网部署、真实外发、付费采购或不可恢复的数据删除。

## 二-A、MIG-002 本地安全基线 Gate

- 一个 Git 根、一个 npm 根、一个 lockfile、一个 `PRODUCT.md`、一个 `PROJECT_LOG.md`；
- 正式 `api-contracts/domain` 不依赖 legacy；legacy 有替换任务且默认不可发布；
- 安装和启动不读取旧 `.env`、`.data`、数据库或用户媒体；
- 只绑定 `127.0.0.1`，外部能力缺配置时明确 unavailable；
- 主动推送、定时外发、微信 notifier、真实 AI/OCR/ASR、S3 和 webhook 在启动图中不可达；
- 固定提交全部受管文件在 manifest 中恰有一个处置；
- `node_modules`、dist、coverage、日志、缓存、数据库和密钥不被 Git 跟踪；
- 两个旧来源的 SHA、状态和文件哈希保持不变。

本 Gate 通过只表示工程收敛且可在本机安全验证，不代表 V1、外部服务、Win11、云端或生产发布完成。

## 三、产品闭环 Gate

必须用合成数据走通：

```text
教师账号开通或受邀加入并登录
→ 建立学生
→ 排课
→ 网页或微信主动输入文字/语音/图片
→ 真实识别
→ 审核并形成业务事实
→ 完课与课时对账
→ 生成家长材料
→ 教师复制或导出
→ 教师可在自行发送后记录发送内容、时间与后续计划
```

每一步同时检查成功、取消、失败、超时、重复、刷新、登录失效和重试。

## 四、发布证据包

每个候选版本至少包含：

- 产品契约版本与本次范围；
- 源码提交与构建镜像摘要；
- 数据迁移与回退说明；
- 自动 Gate 完整结果；
- Windows 11 与手机真实流程截图、执行人、日志和环境摘要；
- OCR、ASR、AI、微信供应商验证记录；
- 跨租户与管理员权限报告；
- 备份恢复、故障和告警演练结果；
- 未关闭问题和风险接受人；
- 产物清单与 SHA-256。

## 五、上线授权

通过全部技术 Gate 只代表“具备上线条件”，不代表已经获准上线。购买资源、办理外部申请、使用真实数据、部署公网和扩大试用仍需用户逐项授权。
