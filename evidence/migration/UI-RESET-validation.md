# V004 教师 UI 清场验证｜2026-09-08

这是一次有明确范围的退役验证，不是新版 UI、完整产品或生产验收。当前任务和进度只见根 PROJECT_LOG.md。

## 用户要求与范围

- 用户要求全部重构前端 UI，先阅读文件、删除当前页面与前端设计说明，避免上下文混乱。
- 本轮仅清理教师端电脑/手机网页。管理员后台是否同步改版曾异步询问，未收到答复，因此保留；不据“平台”一词扩张删除范围。
- 后端运行代码、管理员后台、共享契约、schema/migrations、API 与既有数据不删除、不重写。新原型须用户确认，本轮没有实施新设计。

## 读取与可恢复性

- 对 1,031 个项目自有文件完成全字节读取与哈希清点，其中 1,015 个文本文件；排除依赖、构建产物、密钥、.data 等私有/生成目录。
- 内容审阅覆盖全部 159 个教师前端文件、14 份根 Markdown、27 份 evidence 文档/脚本，并定向检查跨包引用与后端边界。全字节盘点不代表全部后端代码逐行语义审查；二进制截图本轮用于退役定位，不重新做视觉评价。
- 删除前完整备份：`/Users/xiaosi/Developer/artifacts/teacher-platform-formal/ui-reset-2026-09-08-KcX02D/before-ui-reset.tar.gz`。
- 备份 SHA-256：`2103430a0bb114d893b86cbb6aca7df4d8ee125e9d90c2ad375dba4192784dda`。逐文件从压缩包读取后与删除前哈希比对，1,031/1,031 一致，包含未提交改动。
- 恢复只应从备份提取指定路径到另一个空目录核对；不要对当前工作区直接整包覆盖。旧工作空间、Downloads 参考目录和用户生成图片缓存均未删除。

## 退役与保留

- 清单：`evidence/migration/UI-RESET-retired-files.json`，共 146 条退役路径及删除前哈希：116 个前端文件、30 个设计文档/参考索引/截图文件。
- 116 个前端旧路径包含 114 个直接退役文件，以及 2 个混合 TSX 五字段文件退出；后者的纯数据函数与测试迁为 .ts，不保留旧组件。
- 移除 15 个旧 Page 页面、桌面/手机壳、导航路由、旧组件、全部教师 CSS、布局模型和相关测试；其中 37 个旧测试文件退役，旧 App 测试另行替换。测试数量减少不是原功能通过。
- 当前 frontend 共 45 个自有文件：API、TeacherProvider、草稿幂等、上海时间与课程五字段基础保留；入口为无样式、零 API 请求的中性占位。
- 旧 Phosphor 图标依赖从 frontend 声明与 lock 移除；离线且禁用安装脚本，lock 仅该依赖条目与 frontend workspace 条目变化。未删除共享依赖缓存。
- 当前 PRODUCT、README、决策及规划来源中的旧视觉/布局要求已撤销；纯 UI 文档与截图移出源码树。PROJECT_LOG 原始变更与每日历史逐字保留，但明确不作新设计输入；日期固定的旧审计包不重写。
- 保护哈希检查：backend/admin/contracts/api-contracts/domain/ops 及 frontend API 与清场前一致；唯一例外是允许调整的 backend presentation 边界测试，保留共享协议、DTO 和后端安全断言，去除已退役 renderer 的存在性要求。

## 实际验证

| 验证 | 结果 |
|---|---|
| 根 npm run typecheck / lint / build | 均退出 0 |
| frontend npm test | 15 文件、96 用例通过；包含 23 个旧路径仅呈现占位、无业务表单/导航、零 fetch |
| admin lint / test / build | 均退出 0；13 文件、84 用例通过 |
| backend presentation 与 build-runtime 两个边界测试 | 2 文件、17 用例通过 |
| manifest helper 单测 | 3/3 通过；树解析、Git blob hash、精确退役边界 |
| git diff --check | 退出 0 |
| 独立最终复核 | 限定教师 UI 清场 Gate 通过，无必须修正项；不是新版 UI、完整产品或生产通过 |
| 浏览器 | 当前 127.0.0.1:5173/today 仅呈现“教师平台前端重构中”“旧页面已移除，新原型待确认”“后端业务与测试数据保留”；旧工作台未渲染 |
| 跟踪检查 | PRODUCT/PROJECT_LOG 均 V004；36 个永久任务、每行 20 列、有效权重 100、通过权重 34；原始变更和每日历史保留 |
| 迁移清单 | A 1161、B 79、N 17 唯一覆盖；5 分片；826 个目标存在且哈希匹配，旧 UI 处置为 H，不报假 present |
| 文件长度检查 | 退出 1：4 个未改动的后端/schema 既有违规；不降门槛。退役 6 条旧 UI 豁免，剩 20 条登记 |

## 未完成与下一步

- T-036 仅完成清场子项，新原型尚未确认；T-037/T-038 新双端基础、T-018 全页集成未完成。旧网页 Gate 不继承，后端成果不因此归零。
- 不运行或宣称根全量测试通过；此前 5 项后端根失败仍待原任务处理。19 个保留 T 项的既有内容变更如实记入 manifest，不借清场伪称 legacy 已清零。
- 真实 Windows、OCR/ASR/AI、隐私治理、试用、容量、费用和上线仍未通过本次验证。
- 下一步先提交全站电脑/手机新原型供用户确认，再复用保留 API 恢复网页；不自动复用旧图或旧视觉规则。
