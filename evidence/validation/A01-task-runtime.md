# A01 教学任务持久化与恢复验证

本文件保存 2026-09-15 本地合成验证证据；当前执行状态以 [PROJECT_LOG](../../PROJECT_LOG.md) 为准。需求仍为 PRODUCT V009，实施依据为 [A01 契约](../product/A01-task-runtime-contract.md)。

## 验证范围

- 新增 TaskRuntime / StepReceipt、可空旧表关联和事件序号；消息、任务、执行及事件原子保存。
- 同请求重复接收、并发冲突、教师隔离、新旧执行器隔离、原文加密与回读。
- 数据库锁后可信时钟、租约 token/epoch、归档写入保护、只读步骤结果、失败续做与恢复命令幂等。
- 实际 core router 挂载及重建后的持久查询；HTTP 仅接受公共字段，不开放内部测试执行开关。
- 任务分页、事件游标、旧消息 DTO 兼容、迁移与现有完整自动化回归。

真实模型、DSH Session/checkpoint、页面交互、确认正式写事务、来源删除传播、微信和真实设备不属于本批验证。生产装配固定默认 unavailable；test_only 仅由内部测试构造器传入。

## 环境与证据

沿用项目 PostgreSQL 17 隔离 harness，每次创建临时集群并在结束清理；只使用合成数据和临时加密密钥，无真实教师数据库或供应商调用。

原始输出目录：`/Users/xiaosi/Developer/artifacts/teacher-platform-formal/A01`。

| 输出 | 实际结果 |
|---|---|
| migration-focused.log | 2 文件 / 4 项通过：增量迁移保留合成旧消息、执行、确认字段；旧执行器拒绝新版会话及任务 |
| schema-verification.log | 37 项迁移空库应用、49 张表、Prisma CRUD 通过；复制合成库与模型差异为空，15 表计数和 10 类关系保持；退出 0 |
| reader-focused.log | 1 文件 / 2 项通过：跨页无遗漏、并列时间稳定排序、事件全局序号间隔和失效正文过滤 |
| lease-focused.log | 首轮 4 通过 / 1 失败：测试查询 pg_sleep 返回 void 无法由 Prisma 解码；已改为返回整数的等价等待查询，未改时间边界断言 |
| lease-focused-02.log | 1 文件 / 5 项通过：并发认领、锁等待后过期、同执行恢复、旧恢复命令重放、unavailable 与归档/uncertain 护栏；退出 0 |
| focused.log | 综合首轮 28 通过 / 2 失败：测试对原文错误去空白预期、错误要求事件列表只有一项；保留失败输出 |
| focused-02.log | 修正测试为原文逐字相等、唯一 user 回执及独立状态事件断言后，9 文件 / 30 项通过；包含真实 core 挂载与部分完成恢复；退出 0 |

首轮完整 `npm run check`（check.log）退出 1：后端 299 文件/2662 项通过、1 项旧 ToolTurnDto 严格比较失败，实际仅新增 taskId/executionId/seq/eventKind 四个 null 字段。已更新旧契约测试保留全部原字段并精确断言这四项；不是删除测试或放宽比较。后续前端、运维和构建被失败短路，最终完整门禁结果待追加。

## 提交与兼容边界

- 实施基线 `eca4af5`。仓库已有大量未提交业务修改，本批按文件和 hunk 保存 A01 范围，包含启动前已有的 A01 契约草稿并校正实施边界；core 接线仅提交 A01 增量。
- 运维表/迁移计数同步到完整 schema，包括此前已提交迁移对应的计数基线。其余无关业务修改留在工作区。
- 全量检查针对当前工作区；不据此声称所有产品源码均已提交或独立检出状态已完整验证。
- 迁移仅新增结构，旧内容与主键保持。回退代码前停止新版认领，保留新增表和消息；本地 commit 不等于数据回滚、远端 push 或部署。


## 完整检查第二轮的负载问题

`check-02.log` 后端 300 文件/2663 项全部通过；前端 36 文件/220 项通过，`v006-flow.test.tsx` 一项因 5000ms 超时失败，根入口退出 1。管理端、运维和最后构建因此未执行。

`frontend-timeout-focused.log` 中，同文件隔离运行 9/9 通过，测试段约 1.25 秒。基于单跑与整套的差异，限制并行 jsdom worker 到 2 个以减少资源竞争；保留默认 5 秒时限、全部断言和零自动重试。已有 Vite 配置测试补充这些约束。后续以整套前端与完整根入口复验，不能用孤立通过覆盖本次失败。

## 最终结果

- `frontend-workers-focused.log`：37 文件/222 项通过，约 47 秒，退出 0。
- `check-03.log`：第三轮完整 `npm run check` 退出 0，治理、长度、类型、lint、隔离全量测试和全部构建通过。后端 300 文件/2663 项、前端 37 文件/222 项、管理端 13 文件/84 项、运维 124 项通过。
- 运维共 126 项，其中 Windows ZIP/PowerShell Expand-Archive 两项在 macOS 按既有平台条件跳过，未作为 Windows 真实验收。
- 独立只读审阅通过最终拆分、运行隔离、租约、幂等与恢复实现；前端 worker 限制没有更改业务断言、超时或重试。主 Agent 已核对实际日志和退出码。
- 最终文档的治理、治理测试、长度和差异检查见 `final-doc-check.log`。当前实施状态及下游未验证项由 PROJECT_LOG 维护。
- 暂存差异检查规范化了新迁移 SQL 的末尾空行，逐条 SQL 文本保持相同（语句指纹 `f0ede7f6cad33da77b81dadba525412064703242ff390bd59f9ee0f01117e3a4`）；`migration-final.log` 重新从空库应用 37 项迁移并验证旧值兼容，退出 0。最终暂存差异检查通过。
