# GOV-003：文件拆分及迁移声明一致性验证

- 日期：2026-09-15（America/Los_Angeles）；产品要求仍为 V009，关联 F05/F18。
- Git 起点：`7857826`。任务开始时已有大量未提交业务修改；本轮保留它们。
- 环境：macOS、Node 22.16.0、npm 10.9.2、Prisma 6.19.3；数据库验证使用独立临时 PostgreSQL 17.10 和合成资料。
- 当前执行状态以 [PROJECT_LOG](../../PROJECT_LOG.md) 为准；本附件保存本次证据。

## 改动与原有行为

- 媒体服务按文件操作、处理状态、异步任务、依赖装配、参数与公共辅助函数拆开，保留公共构造入口和 13 个方法。
- OCR/ASR 的服务测试与适配器测试拆为四个文件；原有 35 个测试及其断言保留。完整媒体目录还覆盖扫描、加密、租户隔离、存储与证据关联。
- Prisma 入口仅保留 generator/datasource，46 个模型分入六个职责文件；迁移、生成、测试及运维消费者统一使用 schema 目录。七份静态模型测试改为读取完整目录。
- 纯目录拆分阶段，46 个模型正文逐字一致，Prisma datamodel diff 为零。随后补齐既有声明缺口：CaptureTask 的两个生命周期字段补 `@db.Timestamptz(3)`；三条长索引名显式映射到原 SQL 在 PostgreSQL 实际保存的名称。
- 最终模型与拆分前只有上述五处声明差异，涉及三个模型；其余正文一致。这些声明对应既有迁移的实际结构，没有新建或改写 migration，没有转换数据或更改索引字段/唯一性。
- 两个迁移验证脚本共享实际迁移目录计数，避免旧的 15/36 硬编码漂移；缺失 SQL 或空目录仍失败。

## 验证记录

原始输出与拆分前备份均在源码树外：`/Users/xiaosi/Developer/artifacts/teacher-platform-formal/GOV-003/`。

| 检查 | 实际结果 | 原始证据 |
|---|---|---|
| 七份既有模型边界测试及 schema 目录测试 | 8 文件 / 36 项通过，退出 0 | `schema-boundary.log` |
| 目录、动态迁移计数和迁移元数据回归 | 3 文件 / 7 项通过，退出 0；纳入完整后端测试 | 本轮工具输出及最终完整检查日志 |
| 媒体测试直接调用 Vitest | 缺 DATABASE_URL，退出 1；后续改用隔离 harness 验证 | `media-tests.log` |
| 第一轮完整检查 | 两个新增 adapter 测试漏导入初始化函数；发现后补回，终止该轮并清理临时库，退出 143 | `check-01.log` |
| 第二轮完整检查 | 为纳入迁移声明校准而主动终止，退出 143；不作为最终验证 | `check-02.log` |
| 第一次隔离迁移验证 | 空库部署、47 张表和 CRUD 通过；库副本 diff 发现两列类型和三条索引命名差异，整体退出 1 | `migration-verification.log` |
| 校准后的隔离迁移验证 | 36 项迁移部署、47 张表、CRUD、库副本零 diff、15 张源表计数和 10 类关系检查通过；清理成功，退出 0 | `migration-verification-02.log` |
| 最终完整 `npm run check` | 治理、长度、类型、lint、全量测试、构建均通过，退出 0 | `check-03.log` |

最终完整测试：治理 16 项；后端 291 文件/2633 项；前端 37 文件/221 项；管理员 13 文件/84 项；运维 124 项通过，2 项 Windows ZIP/PowerShell 专属测试在 macOS 跳过。跳过不作为真实 Windows 验收。所有最终代码和测试修改均已包含在本轮完整检查中；其后只回填文档并复跑治理、治理测试、长度和差异检查。

迁移验证通过 `runTestsWithPostgres` 的 `childCommandFactory` 顺序执行 `verify-empty-migration.mjs` 和 `verify-existing-baseline.mjs`。只使用该 harness 创建的合成库，未读取真实数据库或真实凭据。最终库副本无待执行 SQL，证明声明校准未要求 ALTER 或索引改名。

模型比较证据：`schema-split-equivalence.json` 保存纯拆分阶段结果；`schema-final-equivalence.json` 保存最终五处声明差异及其余正文等价结果。拆分前 schema SHA-256 为 `085c2d3ac02777e174a79d1d9c8d3e1f0db2e60c060671be9f5486d5f36f0721`。

## Git 与完成边界

- 只按明确清单暂存相关文件。六个模型分片包含任务开始时已存在的 46-model 基线；与之对应的九个既有未跟踪迁移（20260913–20260921）须一起保存，以免只保留声明而遗漏建库依据。它们不是本轮新设计的迁移。
- 相关测试、脚本及模型文件中的既有内容一并保留；其他业务源码、UI 和历史删除仍留在工作区。本次测试验证的是当前完整工作区，不代表已审计或提交此前全部业务改动，也不承诺单独检出此提交即可复现完整产品。
- 既有迁移含账本回填等数据影响；本地提交不执行真实数据库变更，真实部署和数据回滚需单独验证。
- 没有放宽 500 行门禁或修改既有例外、历史档案。工程检查不替代真实模型、微信、Windows、跨设备或用户验收。
