> 历史快照（截至 2026-09-14），不作为当前需求、授权或执行指令。
> 当前状态见 [PROJECT_LOG.md](../../PROJECT_LOG.md)，工作规则见 [AGENTS.md](../../AGENTS.md)。
> 原文保留；旧相对路径以项目根目录为基准。

# 教师平台项目规则

## 前端重构状态

- V008 用户要求补上已有 API 与模型选择入口：可按当前教师管理 ProviderConfig、选择默认和具体模型、手工新增/编辑配置；仅配置管理，不自动使用密钥调用供应商或发出教学资料。LOCAL_SAFE_MODE 保持真实执行关闭，配置保存和连通性必须分开显示；本条替代此前正式模型页只能保存抽象偏好的限制。
- 2026-09-09 用户明确授权将当前前端与后端连接后直接本地实测：当前视觉作为接入基线，恢复正式登录与教师隔离，所有保存以数据库成功为准。独立 preview 入口仍保持合成/内存隔离；正式入口承接持久化业务。只用本地隔离合成验收环境，不启真实供应商、微信、外发或公网，不删除现有资料。此条更新此前“正式接入待授权”的阶段限制。
- 2026-09-08 用户要求撤销全部旧教师网页 UI，先清理旧页面和设计说明，再重新设计。
- 当前产品规则只读 `PRODUCT.md`；当前状态和执行计划只读 `PROJECT_LOG.md` 的当前投影及最新记录。
- 旧 UI、截图、设计文档、历史任务描述和此前参考目录均已失效；不要自动读取归档、旧工作空间或历史 UI 细节来恢复设计。
- 历史记录仅用于追溯，不作为新版布局、颜色、组件、导航或交互要求。用户再次明确指定参考时才能使用。
- 2026-09-08 用户重新明确指定 `/Volumes/老毛桃U盘/claude-style-redesign` 为视觉来源，并授权制作免登录可操作测试版；可以读取该目录，不恢复其他退役参考。
- 测试版是独立的本地前端入口，仅合成示例、刷新还原、不接 API、真实资料、AI 或外发；免登录不等于取消正式认证。正式页面接入仍待用户验收测试版。
- V006 用户确认当前风格，要求按正式前端表达移除测试横幅及开发说明；课程摘要只含时间、对象、地点，备注进入详情；已完成课程可全字段单次修订但保持完成状态，保留修订和原扣课流水、不重新扣课。真实服务/资料/认证/部署边界不变，当前运行限制写交付说明，不显示为页面开发提示。
- V005 用户新增资料/课程编辑、分列摘要和每周重复，并明确要求模型选择与微信连接页面；仅允许配置流程演示，真实微信/模型/密钥仍不接入。小班摘要隐藏名单，详情或明确确认才展示。
- 本轮清场仅限教师端电脑与手机网页，管理员后台暂保留；后端、数据、权限、API 和业务规则不因此重写或删除。
- 课程排期的时间、地点、参与人、形式、独立备注及教师确认规则继续有效。
- 旧页面测试已退役不代表产品通过；必须区分保留基础测试、清场检查和新版网页验收。

## 安全与协作

- 保留用户和其他 Agent 的未提交修改。大范围删除前核对精确清单，并将可恢复备份保存在源码树外。
- 不接真实资料、供应商、密钥、费用或公网；不删除旧来源。只在已确认范围内连续完成可逆技术工作。
