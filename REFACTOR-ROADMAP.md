# 教师 AI 工作平台｜受控迁移路线

> 状态：规划来源稿；当前状态以 `PROJECT_LOG.md` 为准
>
> 执行原则：本项目不是从零重写。先确认产品契约，再按来源 SHA 逐项迁移已证实有价值的产品语义、源码、契约、业务规则和测试证据；能原样迁移就不重写，需要适配时做最小改造，只有不兼容或占位实现才重构替换。
>
> MIG-002 已把工程迁移方法调整为“固定快照一次分类并建立整体可运行基线，随后集中 Debug”。下列 W1–W10 继续表示从 legacy 基线退出到正式产品的后续波次，不再阻止本轮整体基线迁入。

## 一、执行总顺序

### W0｜冻结产品与数据边界

交付：

- 产品能力说明；
- 产品契约；
- 管理员可见范围；
- 原始媒体删除规则；
- 微信产品边界；
- 媒体范围和成本假设；
- 旧资产迁移台账。

完成条件：

- 用户明确确认 V1 整体范围、非范围和验收闭环；
- 关闭媒体删除、管理员矩阵、媒体范围、微信形态与延迟回复、AI/OCR 写入边界、邀请制试用；
- 不把用户未反对或原型中出现的能力视为已确认。

### W0.5｜外部可行性与原创视觉验证

本波可以与 W0 的逐项确认并行启动，但必须在 W1 前完成。

并行交付：

- 中国大陆个人主体、域名和网站手续的官方材料清单；
- 未来微信入口候选与 150 名教师身份绑定的 go/no-go 证据；是否统一入口及具体绑定方式由 T-003 冻结；
- OCR、ASR、AI 的数据地区、保留、费用和退出比较；
- 150 人低、中、高三档容量和预算模型；
- 登录、Today、日程、学生、AI 会话、媒体审核和高影响确认的原创状态图。

Gate：只使用一手资料和合成数据；不采购、不注册、不部署；用户确认关键视觉状态；无法证明微信路径时不得进入 W7。

### W1｜建立正式迁移承载地基

交付：

- 正式迁移目标 monorepo 目录和依赖方向；
- Linux 同构构建；
- 合成数据、统一检查入口和持续集成；
- 代码文件 500 行上限；
- 初始安全、日志、错误和配置边界。

Gate：干净工作区能按锁文件安装、测试、构建容器；教师端和后台产物分离；密钥不进入代码或产物。

### W2｜账号、租户与管理员边界

交付：

- 教师账号、个人工作空间、登录会话；
- 单库多租户隔离；
- 独立管理员账号和审计；
- 停用、退出和会话撤销。

Gate：两个教师的跨租户读取、写入、导出和对象引用全部被拒绝；管理员默认看不到 P-002 禁止的内容。

### W3｜原始事件、媒体和持久化任务

交付：

- 来源事件、媒体元数据、识别版本和审核候选；
- 对象存储；
- 可重启的持久化任务；
- 删除回执、审计和备份恢复底座。

Gate：重复提交只执行一次；进程重启后任务不丢；删除规则可验证；原件删除后仍能追溯提取结果和修订关系。

### W4｜核心教学业务

交付：

- 学生档案与时间线；
- 今日与周期日程、改期、取消、恢复和冲突；
- 课堂记录、备忘、沟通记录；
- 缴费和课时追加流水。

Gate：用合成数据走通建学生、排课、完课、记录、缴费与课时对账；高影响修改不能绕过确认。

### W5｜迁移教师网页能力并重构页面实现

设计前置：

- 完整读取用户指定视觉原型的页面源码；
- 逐页登记旧教师前端的来源 SHA，迁移功能、流程、状态、文案语义和异常路径；
- 提炼色彩、字体、留白、形状、信息层级、动效和组件规律；
- 将原型的美术风格与正式产品功能分开，不继承其功能缺口或旧技术实现；
- 将 `UI-VISUAL-CONTRACT.md` 作为视觉和交互真源；
- 日程默认采用“月历概览 + 选中日期 + 当日纵向课程列表”，周表降为次级视图；
- AI 助手桌面使用可收起左侧会话栏，手机使用覆盖式会话抽屉；
- 先提交关键页面的视觉方向和真实尺寸截图，由用户确认后再批量开发。

首批页面：

1. 登录与 Session 壳；
2. Today；
3. 日程；
4. Students / Student detail；
5. 上下文 AI；
6. 真实 URL 路由和共享组件。

随后逐步补齐待审核、课后记录、缴费、反馈、隐私和设置。旧 TSX/CSS、内存路由和旧视觉实现不直接复制，但页面业务语义必须迁移；是否清理旧仓库由用户另行授权。

Gate：刷新、深链和前进后退可用；日程选中的日期可由地址恢复，手机默认不依赖横向周表；AI 会话切换不混淆草稿、学生和待确认操作；Windows 11 Edge/Chrome 和 375–430px 手机网页无页面级横向溢出；键盘、焦点、44px 触控、WCAG AA 和草稿恢复通过。

### W6｜真实 OCR 与真实 ASR

交付：

- 至少一个可用于预发布的真实 OCR 和 ASR 供应商；
- 固定样本、可信度、失败重试和人工修订；
- 多图批次与媒体删除流程；
- 费用和异常用量保护。

Gate：真实供应商环境覆盖成功、低可信、失败、重试、重复、删除和换供应商；任何占位适配器不能进入发布配置。

### W7｜迁移并重构微信入口

交付：

- 每教师微信身份绑定、暂停、解绑和重绑；具体绑定方式按 T-003 与真实供应商 Gate 冻结；
- 私聊文字、语音和图片入站；
- 仅回复当前教师请求；
- 幂等、断线恢复和紧急停用。

Gate：两个教师绑定隔离；重复消息不重复执行；未绑定、暂停、群聊和主动发送路径全部拒绝；微信故障不影响网页。

### W8｜AI 整理与家长反馈

交付：

- 受控提取、待审核、学生时间线；
- 至少一个可用于预发布的真实 AI 供应商和固定评测样本；
- 证据准入、九部分家长报告、编辑、复制和导出；
- 沟通结果与后续计划。

Gate：真实 AI 覆盖输出安全、超时、限流、失败恢复、数据地区、保留、费用上限和停用路径；占位 AI 在预发布和试用环境 fail-closed；只有已确认且允许家长查看的事实进入草稿；矛盾不自动裁决；每段内容可追溯；不存在直接发送接口。

### W9｜后台、隐私与运维

W2 后可以先实现管理员身份和审计壳；以下完整能力必须等 W3、W4、W6、W7、W8 的数据和状态稳定。

交付：

- 独立管理后台；
- 账号、用量、故障、成本和用户反馈；
- 教师导出、更正、注销和删除流程；
- 备份、恢复、监控和告警。

Gate：管理员越权矩阵、授权排障审计、导出完整性、注销行为、备份恢复和告警演练全部有证据。

### W10｜云端候选版与试用

交付：

- 预发布环境；
- 同一镜像提升与回退；
- 3–5 人 Alpha、8–15 人 Beta；
- 150 人容量和费用模型；
- 上线决策包。

Gate：完整闭环、容量、成本、恢复和回退通过；跨教师泄露、非预期高风险写入、主动微信发送均为零；用户明确批准后才能正式上线。

## 二、依赖关系

```text
W0 ───┐
      ├→ W1 → W2 → W3 → W4 → W5
W0.5 ─┘        W3 → W6 → W7
               W3 + W4 + W6 → W8
               W2 → W9 基础身份与审计壳
               W3 + W4 + W6 + W7 + W8 → W9 完整后台
               W5 + W7 + W8 + W9 → W10
```

## 三、每一波的固定工作方式

1. 写明该波的用户结果、范围外事项和验收标准。
2. 从旧项目迁移候选索引中只选择当前依赖的资产，并登记旧提交 SHA、源路径、目标路径和对应测试。
3. 先迁移证据测试和可安全复用实现；不兼容部分才做适配或重构，并保留行为对照证据。
4. 开发 Agent 与独立审查 Agent 分离；测试不能只由实现者自证。
5. 先完成单元和契约 Gate，再完成集成、真实浏览器和生产同构 Gate。
6. 把失败证据、修复和最终证据写入验证记录。
7. 涉及真实账号、费用、数据、外发、部署或删除时，单独取得用户授权。

## 四、当前暂停点

A1 已完成，W0 尚未全部完成。完整 W1 以及会固化微信、媒体、管理员或 AI 行为的模块，仍需关闭对应产品与外部 Gate。依据用户 2026-09-01 的新决定，来源已锚定、且不固化未决产品行为的基础切片可以先迁移；当前状态与例外只以 `PROJECT_LOG.md` 为准。

当前可以继续：T-012 迁移来源锚定、符合上述边界的基础迁移、逐项产品确认、只读供应商调研、容量模型、原创状态图和 RC2 历史固化方案。当前不能开始：未锚定资产迁移、会固化未决产品行为的代码、Git push、采购、真实账号、真实数据和公网部署。
