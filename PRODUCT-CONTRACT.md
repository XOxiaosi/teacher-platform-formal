# 教师 AI 工作平台 V1｜产品契约

> 状态：规划来源稿；A1 已确认，A2–A7 继续确认
>
> 唯一产品事实源已迁入 `PRODUCT.md`。本文件保留详细来源与待确认问题，不再单独维护当前产品状态；冲突时以 `PRODUCT.md` 和用户后续确认记录为准。
>
> 2026-09-01 的 MIG-002 只改变工程迁移方式：旧固定快照整体进入一次处置批次，`legacy-only` 应用可临时承载构建基线；这不改变本文件的产品边界，也不把旧实现升级为需求。

## 一、用户原始要求

- 单独建立正式版工作空间，从旧项目中一点点迁入有价值的内容。
- 旧教师前端页面实现不直接迁入；先逐页提取有效功能、流程、状态和异常语义，再在正式教师端适配或重构页面实现。
- 正式版运行在云端，教师通过网页使用，平台管理员通过网页查看运营数据。
- 第一版包含微信、真实语音转文字和真实图片文字识别。
- 平台没有主动或自动发送微信消息的能力。
- 部署地区为中国大陆，资源申请主体为个人。
- 第一期按 150 名个人教师规划。
- 每位教师扫码绑定自己的个人微信，由微信 Bot 响应教师主动发起的交互。（原始表述；D-013 已明确当前只预留未来入口，具体绑定方式尚未确认。）
- 原始音频和图片处理成功后删除；处理失败时保留，用于追溯和重试。

## 二、产品目标

为中国大陆个人教师提供一套持续运转的 AI 教学工作平台，把散落在微信、语音、截图、表格和记忆中的材料，整理成可查询、可修改、可追溯的学生档案、课程记录与家长沟通依据。

V1 的核心价值是：

> 低负担记录 → 形成可信教学档案 → 支撑日程、课时和家长反馈 → 教师核对后自行使用。

## 三、目标用户

### 教师

- 中国大陆个人教师。
- 第一期按 150 名教师规划。
- 每位教师拥有互相隔离的个人工作空间。
- 微信用于快速输入和查询；网页用于完整核对、编辑、审核、导出和管理。

### 平台管理员

- 负责教师账号、用量、问题、运行状态、数据保障和成本管理。
- 使用独立管理入口，不能直接复用教师身份。

### 家长

- 家长不是 V1 平台用户。
- 平台只协助教师准备沟通内容，不建立家长端，不直接联系家长。

## 四、教师要推进的任务

当教师刚下课、收到家长消息、调整课程或需要汇报学生情况时，希望用尽可能少的输入完成记录和查询，让平台持续保存教学事实、发现遗漏，并在需要时快速形成有依据的反馈，而不必重新翻找微信和依赖记忆。

## 五、核心使用场景

1. 教师在微信中主动发送文字、语音、成绩截图、聊天截图或作业图片。
2. 平台完成真实语音转文字或图片文字识别，判断学生、日期、材料类型和可能的业务事实。
3. OCR、ASR 和 AI 结果先形成候选；只有教师确认或进入已冻结的低风险自动写入白名单后，才能成为对应业务事实。
4. 教师在网页查看今日课程、改课、冲突、待办、课后记录和课时余额。
5. 完课后补充课堂状态、作业、薄弱点和下一步计划。
6. 平台持续形成学生长期档案，并保留来源、AI 整理结果和教师修订关系。
7. 教师根据可信记录生成家长反馈或阶段报告，核对后复制或导出，自行发送。
8. 教师记录实际沟通结果、双方共识和下一次跟进事项。
9. 微信、AI 或识别服务暂时不可用时，网页中的基础管理仍可继续。

## 六、核心产品行为

1. 每条输入先建立来源记录，再进行识别和整理；派生结果不得覆盖来源事实。
2. 原始文字、识别结果、AI 提取、教师确认内容和后续修订分层保存。
3. 原始音频、图片文件按删除规则物理删除，但来源指纹、提取文字、处理状态和修订历史继续保留。
4. 处理失败或结果不确定时保留原始文件，允许查看原因和重新处理。
5. 来自语音识别、图片识别、模糊匹配或 AI 推断的内容，存在学生、日期、角色或数值歧义时必须待审核。
6. AI 不能把猜测直接写入某个学生，也不能覆盖已经确认的事实。
7. 涉及学生稳定资料、缴费和重要日程的修改，执行前让教师看清对象、变化和影响。
8. 家长反馈只使用已确认、允许对家长表达且未被后续版本替代的记录。
9. 内部教学判断、待核对内容和敏感背景默认不能进入家长材料。
10. 微信 Bot 只能响应已绑定教师主动发起的私聊请求。
11. 禁止定时微信推送、无请求通知、批量发送和主动联系家长。
12. 平台内可以显示到期事项、早间简报和晚间回顾入口，但不能因此自动发送微信。
13. 微信与网页共享同一份业务事实，不能形成两套数据。
14. 失败必须显示为失败或待处理，不能伪装成功；重复消息不得重复写入。
15. AI 的不同会话必须隔离草稿、学生上下文和待确认操作；切换会话不能造成跨学生误操作。

## 七、V1 范围

- 教师账号开通、登录、停用、注销和个人工作空间；采用邀请制还是开放注册仍待确认。
- Windows 11、常见电脑浏览器和手机网页。
- 未来个人微信身份绑定、暂停、解绑和重新绑定；具体绑定方式仍待确认，当前迁移阶段不开放。
- 教师主动私聊中的文字、语音、图片输入与查询。
- 今日工作台、月历、按日课程清单和周课程表；手机默认按日查看，周表作为次级视图。
- 课程新增、改期、取消、恢复、完成和冲突提醒。
- 学生档案、长期时间线、成绩、作业、课堂观察、沟通和待审核区。
- 课后记录、缴费和课时余额。
- 真实语音转文字、真实图片文字识别和失败重试。
- AI 查询、材料分类、记录整理和受控操作。
- AI 多会话、会话列表、选择和切换；新建与搜索为推荐最小能力，其他管理动作在 W5 前冻结。
- 家长反馈草稿、九部分沟通报告、证据核对、编辑、复制和导出。
- 备忘、平台内提醒和每日回顾。
- 教师数据查看、更正、导出、撤回授权和删除申请。
- 用户反馈、故障恢复和独立管理后台。
- 数据备份、恢复演练、成本与异常用量监控。

默认九部分家长沟通报告：

1. 封面；
2. 学业表现；
3. 家长反馈；
4. 问题诊断；
5. 学习建议；
6. 习惯与心理建议；
7. 双方共识；
8. 后续计划；
9. 备注与内部归档。

## 八、V1 明确不包含

- 家长登录端、家长小程序和平台直接联系家长。
- 原生手机应用。
- 群聊、朋友圈、自动加好友和营销群发。
- 任何主动或自动微信推送。
- 多教师共享学生、团队任务分工和复杂权限。
- 在线收款、退款、分账和教师联盟分成。
- 自动阅卷、自动判分和题目级推荐。
- 视频、动图和大批量媒体导入。
- 自动生成营销内容。
- 多个 AI 自主执行、向量数据库和第三方开放平台。
- 整包迁移旧前端，或把旧项目的完成状态当成正式版要求。

## 九、约束与上线前提

- 中国大陆个人主体的域名、网站手续、微信入口及外部 AI、语音识别和图片识别服务，必须在采购前依据执行时有效的官方要求和供应商条款核验。
- 未成年人资料、家长聊天、录音和截图进入外部服务前，必须完成知情、授权、用途和数据处理范围设计。
- 费用取决于教师平均学生数、语音分钟数、图片量、AI 消耗、失败材料保留期和同时使用人数。
- AI 和识别服务不可用时，基础学生、课程、缴费和人工记录能力必须继续可用。
- 原始媒体删除后，只能依据提取文字、来源指纹、处理状态和修订历史追溯，不能再回放原音频或查看原图。
- 正式上线前必须经过接近正式环境的预发布验证、备份恢复、权限隔离、成本上限和白名单试用。
- 本机时间不能作为认证、审计和日程的可靠依据，相关时间以可信服务端时间为准。
- 单文件超过 500 行时先拆分，不继续叠加功能。

## 十、产品验收标准

1. 两个教师账号互相不能查询、导出或修改对方任何资料。
2. 教师能完成“建学生—排课—微信或网页输入—真实识别—审核归档—完课—核对课时—生成家长反馈—手动发送—记录跟进”的完整闭环。
3. 未来微信身份绑定、暂停、解绑、重绑和重复消息拦截通过；具体绑定方式按 T-003 最终确认。
4. 微信只在教师主动请求后回复，系统不存在定时、批量或无请求发送路径。
5. 真实供应商环境覆盖语音和图片的成功、低可信、失败、重试和删除流程。
6. 学生歧义、日期歧义、低可信转写和图片中的其他学生信息不能自动成为正式记录或家长反馈依据。
7. 任何正式学生记录和家长反馈都能查看来源、处理状态和修订关系。
8. 缴费、学生稳定资料和重要日程修改不能绕过教师确认。
9. 刷新、断网、登录过期、AI 超时、识别失败和部分完成时，草稿及已成功工作不静默丢失，也不重复执行。
10. 教师能够查看、更正、导出和申请删除自己的数据。
11. 核心流程在 Windows 11 桌面浏览器和 375–430px 手机网页通过真实操作验收。
12. 上线前必须有预发布部署、升级、回退、恢复、监控、容量和费用控制的真实证据。
13. 跨教师访问、非预期高风险写入和主动微信发送必须为零。
14. AI 会话切换、刷新和生成中离开后，草稿、学生上下文与待确认操作不能串到其他会话。

## 十一、已经确认

- A1 的平台目标、服务对象、V1 模块、完整闭环和明确非范围；
- 项目是逐能力受控迁移，不是从零重写；
- 中国大陆部署；
- 个人主体持有资源；
- 第一期 150 名个人教师；
- 当前先迁移网页与核心平台，微信真实接入后置；具体绑定方式尚未确认；
- 微信 Bot 只响应教师主动发起的对话；
- 无主动或自动微信发送能力；
- V1 使用真实语音转文字和真实图片文字识别；
- 原始音频和图片处理成功后删除，失败时保留追溯和重试；
- 家长内容由教师核对、复制或导出后自行发送；
- 家长端、团队协作和在线收款不属于 V1。

不整包迁移旧前端不等于放弃旧页面承载的有效产品语义；这些语义必须先进入迁移台账和目标契约。

## 十二、待用户确认

### P-001｜原始媒体完整生命周期（P0）

推荐方案：

- 识别成功后进入待核对状态；
- 教师确认结果后立即删除原件；
- 待核对原件最长保留 7 天，逾期删除但结果继续保持待审核；
- 识别失败原件最长保留 30 天用于重试，教师可以提前删除。
- 缩略图、临时文件和缓存随原件删除；删除失败必须告警、重试并留下回执；
- 备份中的原件按冻结后的退出周期淘汰；供应商副本保留期必须在采购前核验；
- 只允许导出仍在保留期的原件，删除后导出提取结果、来源指纹、处理状态和修订历史。

影响：它决定隐私风险、存储费用，以及教师发现识别错误后能否复核原件。

### P-002｜管理员可见范围（P0）

推荐方案：

- 管理员默认只看账号、用量、故障和服务状态；
- 默认不能浏览具体学生内容；
- 只有教师针对具体问题明确授权后，才允许限时、限范围访问；
- 所有支持访问都保留记录。

冻结时需分别列清：全平台聚合、单教师账号与用量、具体教学内容、限时支持访问、导出、修改、停用和恢复权限。

影响：它决定隐私承诺、客服排障方式和管理后台验收标准。

### P-003｜V1 媒体能力承诺（P1）

推荐方案：

- 包含微信短语音、网页语音输入、长录音上传、聊天截图、成绩截图、作业和试卷图片及连续截图；
- 不包含视频、动图和大批量文件；
- 单文件和单批数量上限在供应商费用评估后冻结。

影响：取消长录音或连续截图可以显著降低处理成本和验收复杂度。

### P-004｜微信绑定形态与延迟回复（P0）

推荐方案：教师用个人微信扫码，把身份绑定到一个统一的平台 Bot；平台不登录或托管教师个人微信。语音或图片处理完成后的延迟回复必须绑定原入站消息、受有效期限制，不能形成通用主动推送。

影响：这决定微信供应商、账号风险、150 人容量和“只响应教师请求”的验收定义。

### P-005｜AI/OCR 派生事实如何写入（P0）

推荐方案：V1 所有 OCR、ASR 和 AI 派生的学生事实先进入候选区，教师确认后才进入正式记录；查询可以直接回答，高影响写入必须确认。

影响：这决定错误识别是否可能污染学生档案，以及审核区的工作量。

### P-006｜注册与首批试用方式（P0）

推荐方案：V1 先采用邀请制；Alpha 3–5 人、Beta 8–15 人，通过后再讨论开放注册。

影响：这决定账号开通流程、客服范围、真实数据暴露面和费用增长速度。
