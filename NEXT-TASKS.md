# 教师 AI 工作平台｜下一步任务清单

> 状态：W0 规划执行中，尚未授权正式功能开发
>
> 本文件回答“从现在开始先做什么”。详细长期路线仍在 `REFACTOR-ROADMAP.md`；发生冲突时，以产品契约、决策记录和本文件中最新的当前任务为准。

## 一、当前所处位置

已经完成的规划资产：

- 独立正式版工作空间；
- 第一版平台能力说明和产品契约草案；
- 正式版架构、旧资产迁移台账和独立验收 Gate；
- 暖色编辑式视觉方向；
- 日程“月历概览 + 当日议程”结构；
- AI 助手桌面会话栏和手机会话抽屉方向。

尚未完成：

- V1 整体范围和非范围尚未得到最终确认；
- 微信绑定形态、媒体删除、管理员权限、首批媒体范围、AI/OCR 写入边界和注册方式尚未冻结；
- 豆包、日程截图和旧视觉原型只是参考，正式页面还没有用户确认过的原创状态图；
- 个人主体在中国大陆使用云端、微信、OCR、ASR 和 AI 的当前可行性与费用尚未形成一手证据包；
- 正式工程仓库尚未初始化，旧 RC2 历史也尚未获准推送远端固化。

因此，当前不进入正式功能开发。

## 二、总执行顺序

```text
A｜关闭产品决策包 ───────────┐
B｜外部可行性与容量调研 ─────┼→ E｜冻结产品、架构与正式仓库
C｜原创 UI 关键状态验证 ─────┤          ↓
D｜准备 RC2 历史固化方案 ─────┘      F｜工程地基、账号与数据底座
                                          ↓
                                   G｜教学核心与首个可用预览
                                          ↓
                                   H｜真实 OCR/ASR、微信与 AI
                                          ↓
                                   I｜后台、隐私、运维与预发布
                                          ↓
                                   J｜Alpha、Beta 与上线授权
```

A、B、C、D 可以在不写正式功能代码、不采购、不部署的前提下并行。E 必须等 A 关闭，并吸收 B、C、D 的结果。

## 三、现在开始的四组任务

### A｜关闭产品决策包

用户不需要一次回答一长串问题。Agent 按影响顺序一次只提交一个“事实—推荐—影响—风险—确认项”。

#### A1｜确认 V1 目标与模块清单

交付：确认平台目标、服务对象、V1 模块清单、明确非范围和完整业务闭环，收集需要修改的地方。

Gate：用户明确接受总体方向；文档继续保持“待确认”，直到 A2–A6 关闭并通过 A7 最终汇总确认。

#### A2｜确认微信产品形态

需要区分两种完全不同的产品：

1. 推荐：教师用自己的微信扫码，把身份绑定到一个统一的平台入口，并主动联系平台 Bot；平台不登录或托管教师个人微信。
2. 高风险方案：平台登录并长期托管每位教师自己的个人微信账号。

还需确认：教师发送语音或图片后，识别完成的延迟回复是否仍视为对这条入站请求的响应。推荐允许，但必须绑定原入站消息、显示处理中，并设置明确有效期；不能借此发展成通用主动推送。

Gate：微信身份、入站、延迟回复和“绝不主动发送”的验收定义没有歧义。

#### A3｜冻结媒体完整生命周期

在现有 P-001 推荐基础上补齐：

- 成功、低可信、失败和教师未核对分别何时删除；
- 原始文件、缩略图、临时文件、缓存、备份和供应商副本分别如何退出；
- 删除失败如何告警、重试并留下回执；
- 原件仍在保留期时可以导出，删除后只导出提取结果、指纹、状态和修订历史。

Gate：任何媒体都能回答“现在存在哪里、谁能看、最晚何时删除、删除失败怎么办”。

#### A4｜冻结管理员与注册边界

权限矩阵至少分为：

- 全平台聚合统计；
- 单个教师的账号、用量和故障元数据；
- 具体学生和教学内容；
- 教师授权后的限时支持访问；
- 导出、修改、停用和恢复权限。

推荐 V1 采用邀请制注册：Alpha 3–5 人、Beta 8–15 人，验证后再讨论是否开放注册。

Gate：教师入口和管理员创建/邀请教师的关系一致；管理员默认不能浏览具体教学内容。

#### A5｜冻结首批媒体范围

推荐包含微信短语音、网页语音、长录音、聊天/成绩/作业/试卷图片和连续截图；不含视频、动图和大批量文件。单文件、单批和每月上限在费用调研后冻结。

Gate：每种承诺材料都有输入上限、成功标准、失败路径和人工修订方式。

#### A6｜冻结 AI/OCR 写入边界

推荐 V1 所有 OCR、ASR 和 AI 派生的学生事实先进入候选区，教师确认后才进入正式记录；教师明确发起的查询可以直接返回，但高影响写入仍需确认。

交付：形成“只能建议、需要确认、允许直接执行、明确禁止”的操作矩阵。

Gate：不存在无法解释的“信息足够明确就自动写入”。

#### A7｜汇总确认并冻结 V1

交付：把 A1–A6 的结果统一写回能力说明、产品契约和决策记录，消除冲突后提交一份最终确认稿。

Gate：用户明确确认最终版本，`PRODUCT-CAPABILITIES-V1.md` 和 `PRODUCT-CONTRACT.md` 才能从“待确认”改为带版本的“已确认”；不能用“没有反对”代替签字。

### B｜外部可行性与容量调研

只读核验，不注册账号、不提交实名、不购买额度。

并行形成四份决策材料：

1. 中国大陆个人主体的云账号、域名、网站手续和隐私前提；
2. 统一平台微信入口、教师扫码绑定、延迟回复和 150 人规模的真实可行路径；
3. OCR、ASR、AI 候选服务的数据地区、原件保留、准确度、延迟、费用、停用和退出方式；
4. 150 名教师的低、中、高三档用量与月预算模型。

Gate：关键结论只使用执行时有效的官方或供应商一手材料；至少保留两种可退出方案。微信路径若无法证明可行，必须在写 W7 前回到用户调整产品承诺。

容量调研需要补充的输入：试用人数、每位教师平均学生数、每月语音分钟数、图片量、AI 会话量、同时在线人数和预算上限。未知数据可以先设三档假设，但必须标明是假设。

### C｜原创 UI 关键状态验证

只使用合成教师和合成学生数据，不连接旧前端或正式后端。

第一批状态图：

1. 登录与 Session 失效；
2. Today；
3. 日程桌面“月历 + 当日议程”、手机日程、冲突和无课；
4. Students 和 Student detail；
5. AI 桌面会话栏展开/收起、手机抽屉打开/切换完成；
6. 媒体识别待处理、待核对、失败和删除倒计时；
7. 高影响操作确认和部分成功。

每个核心页面至少覆盖 `1440×900`、`1366×768` 和 `390×844`；额外检查 375、430px、Windows 11 字体、键盘、焦点、44px 触控和 WCAG AA。

Gate：用户确认信息结构和视觉方向后，才允许 W5 把这些状态实现为正式页面。参考源码、截图、豆包品牌和未授权素材都不进入公开仓库或产物。

### D｜准备 RC2 历史固化方案

先只读登记：旧仓库根、当前 HEAD、远端差异、本地 RC2 tag、三件套路径与 SHA-256。

只读登记必须先重新确认提交数量、每个提交 SHA、tag 指向和远端地址，不能只凭既有审计摘要执行。

推荐目标是旧项目当前远端中的专用 `archive/windows-rc2` 分支和 RC tag，绝不能推入正式版远端；如果用户不希望旧远端承载归档，则先由用户另选独立私有归档远端。授权问题必须写清“目标仓库、目标分支、tag 和 SHA”。不直接把旧 RC2 合入正式版主线，也不把 `dist`、zip 或旧数据库复制到新项目。

Gate：在迁移任何旧规则前，历史来源能由 Git 提交和 tag 复核；执行 push 前仍需用户明确授权。

## 四、产品契约冻结后的工程任务

### E｜冻结正式事实源并初始化工程

- 关闭 A1–A7；
- 吸收外部可行性和原创 UI 结论；
- 冻结架构、数据责任、运行环境和验收 Gate；
- 用户授权后初始化正式 Git 仓库，决定远端和 RC2 历史处理；
- 固化 `.gitattributes`、跨平台脚本、显式前端 root 和 `strictPort`。

Gate：干净工作区只有一条产品真源、一条执行计划和一套 Gate；旧前端、RC2 包和敏感数据没有进入正式工程。

### F｜W1–W3：工程地基、账号隔离和数据底座

1. W1：新 monorepo、Linux amd64 同构构建、合成数据、CI、安全与 500 行 Gate。
2. W2：教师登录、Session、单库多租户、独立管理员身份和审计。
3. W3：原始事件、对象存储、持久化任务、审核候选、删除回执和备份恢复底座。

Gate：干净 Linux 环境按锁文件安装、测试和构建；两个教师之间的读取、写入、导出和对象引用全部拒绝；任务重启不丢、重复提交不重复写入。

## 五、首个可用预览

### G｜W4–W5：教学核心与正式教师网页

先冻结学生、课程、日程、课时、课堂记录和 AI 会话的运行时契约，再并行实现领域规则和教师网页。

开始 W4/W5 前还需关闭一组模块级产品规则：

- 周期课程修改作用于本次、以后还是整组；
- 取消、请假、未到课和补课是否消耗课时；
- 缴费流水与课时增减如何对账；
- AI 会话的最小管理动作、保留、归档、删除和导出含义；
- AI 操作矩阵中哪些只能建议、哪些必须确认；
- 家长报告的内部归档部分默认不进入家长版复制和导出。

这些规则不阻塞 W1–W3 的通用地基，但未关闭时不能实现对应 W4/W5 行为。

首个可用预览必须具备：

- 真实 Session 和真实 PostgreSQL；
- 只使用合成数据；
- Today、学生/详情、月历与当日课程、基础缴费课时账本；
- AI 会话新建、列表、选择、切换、草稿和学生上下文隔离；
- 真实 URL、刷新、深链、前进后退和草稿恢复；
- Windows 11 Edge/Chrome 与手机网页真实操作证据。

真实 AI、OCR、ASR 和微信尚未接入时，界面必须明确显示“交互评审模式”，不能用模拟成功冒充 V1 已完成。

Gate：用合成数据走通“登录—建学生—排课—完课—记录—缴费课时对账”，并通过独立产品与工程审查。

## 六、外部能力与完整 V1

### H｜W6–W8：真实识别、微信与受控 AI

1. W6：真实 OCR/ASR、固定样本、低可信、失败、重试、人工修订和费用保护。
2. W7：统一平台微信入口、教师绑定、文字/语音/图片入站、回复原请求、解绑和紧急停用。
3. W8：真实 AI 供应商、受控 AI、待审核、证据快照和九部分家长报告。

W8 同时依赖 W3 原始事件、W4 已确认业务事实和 W6 真实识别；不能只依赖 W6。真实 AI 必须覆盖固定评测样本、输出安全、超时、限流、失败恢复、数据地区、服务方保留、费用上限和停用路径；占位或模拟 AI 在预发布、Alpha 和正式环境必须 fail-closed。家长版复制或导出必须默认排除“内部归档”部分，平台不验证或声称实际发送成功，只允许教师记录已人工发送。

Gate：真实供应商覆盖成功、低可信、失败、超时、限流、重复和恢复；主动微信发送路径为零；所有家长材料可追溯且只能复制或导出。

### I｜W9：后台、隐私与运维

管理员身份与审计壳可以在 W2 后开始。完整后台中的教学业务统计、外部服务用量/故障/成本、用户反馈、导出、注销、备份恢复和监控必须等 W3、W4、W6、W7、W8 对应数据和状态稳定。

Gate：管理员权限矩阵、教师数据权利、导出完整性、备份恢复、告警和受控运维作业全部通过；后台 HTTP 不能直接执行系统命令。

## 七、云端试用与上线

### J｜W10：预发布、Alpha、Beta 和正式授权

1. 在固定 Linux 环境构建一次候选镜像；
2. 预发布通过迁移、回退、备份恢复、容量、成本和完整 Gate；
3. Windows 11 从浏览器访问同一候选产物并回传截图和日志；
4. 用户另行授权后开展 3–5 人 Alpha；
5. 高风险问题关闭后扩大到 8–15 人 Beta；
6. 以 150 人容量模型复核成本和扩容阈值；
7. 用户再次明确授权后才允许正式上线。

Gate：跨教师泄露、非预期高影响写入和主动微信发送均为零；同一镜像可回退，备份真实可恢复，费用不超过用户确认的上限。

真实 Windows 11 证据不能由 macOS 模拟替代。推荐由用户的 Win11 电脑或指定测试人运行 Agent 提供的一键验收包，生成日志、浏览器截图和环境摘要，再由独立 Reviewer 复核；执行人和证据回传方式在预发布前冻结。

## 八、用户只需要做的决定

用户不负责选择框架、数据库、接口形式、目录结构或测试工具。只在以下节点决策：

1. 接受或修改产品边界；
2. 确认用户能看见的关键页面和流程；
3. 确认隐私、媒体保留、微信和管理员边界；
4. 确认供应商锁定、预算和付费采购；
5. 授权 Git 推送、真实账号、真实数据、预发布、试用和上线。

## 九、现在明确不做

- 不清理或删除旧项目；
- 不迁入旧教师前端、旧迁移链、RC zip 或 `dist`；
- 不执行 Git push、建远端或合并 tag；
- 不购买服务器、域名和供应商额度；
- 不提交备案、实名或审核资料；
- 不录入真实教师、学生、家长、语音或图片；
- 不部署公网；
- 不把占位 OCR/ASR/AI 或模拟微信标成可用；
- 不引入微服务、Kubernetes、RabbitMQ 或向量数据库。

## 十、当前下一项

下一项不是写代码，而是由 Agent 按 A1–A7 顺序制作产品决策包，并一次只请用户确认一个会改变产品结果的问题。同时可并行准备 C 的第一轮原创状态图和 B 的外部调研提纲。
