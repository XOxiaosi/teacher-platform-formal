# 教师网页视觉与交互契约

> 状态：视觉方向、日程结构与 AI 会话侧栏已确认，正式页面尚未设计
>
> 本文件约束教师网页的视觉语言和关键交互。平台能做什么只以 `PRODUCT.md` 为准；参考原型中出现的功能不能自动进入正式版。

## 一、参考来源及用途

### 全局视觉母版

- 源码：`/Users/xiaosi/Downloads/claude-style-redesign`
- 已逐文件通读：2 份设计与交互说明、4 份页面脚本、共享样式、共享脚本、构建脚本、静态验证脚本和生成后的单页文件。
- 用途：提取色彩、字体、留白、层级、形状、组件气质、动效和响应式规律，再独立设计正式版。
- 不作为：产品范围、数据模型、权限、业务状态、外部发送能力或工程实现的事实源。
- 权利边界：原型没有授权文件，按 `license: UNKNOWN` 和内部参考处理；源码、图标、文案和截图不得进入公开仓库或产品产物。

### 日程模块结构参考

- 原图：本机内部参考文件 `evidence/ui-reference/12-schedule-mobile-structure-reference.jpg`。
- 用途：参考“月历概览 + 摘要 + 选中日期 + 当日纵向课表”的信息结构。
- 边界：只借鉴结构和阅读顺序，不照搬粉色高饱和配色、财务数字、实时动态入口、五项底部导航、品牌元素或水印。

### AI 助手会话导航参考

- 参考对象：豆包手机版的会话入口与侧栏交互，不复制其品牌、配色、图标、文案或完整布局。
- 当前公开证据：[豆包 App Store 页面](https://apps.apple.com/cn/app/%E8%B1%86%E5%8C%85-%E9%9A%8F%E6%97%B6%E5%B8%AE%E5%BF%99%E7%9A%84-ai-%E5%8A%A9%E6%89%8B/id6459478672)展示了聊天页左上菜单入口和右上新会话入口；公开营销图未展示展开后的完整会话栏。
- 执行边界：展开栏的内容结构依据教师平台自己的学生上下文、待确认操作和会话安全规则独立设计，不能把推测的豆包细节写成事实。

## 二、正式版独立视觉方向

正式版保留以下视觉特征：

1. 以暖纸张色作为页面画布，不做冷白或蓝灰 SaaS 后台。
2. 页面标题、长文和关键数字使用中文衬线字体；导航、表单和操作使用清晰的无衬线字体；时间和技术状态才使用等宽字体。
3. 通过暖米色表面、白色密集列表和暖黑 AI 工作面建立层次，不依赖大阴影。
4. 珊瑚色只用于少量主操作、当前状态和关键链接，不把整页染成粉红色。
5. 通过细边线、留白和内容密度建立秩序；卡片圆角克制，不做悬浮玻璃拟态。
6. AI 的“建议—教师核对—确认执行”使用独立暖黑卡片，和普通业务信息明显区分。
7. 桌面保持编辑台般的宽松阅读节奏；手机重新编排信息，不把宽表格等比例缩小。
8. 状态必须同时有文字或图标，不能只靠颜色表达。

## 三、基础视觉令牌方向

正式版独立建立一套可访问的色值，不逐项复制参考原型。语义色板固定为：

- 暖纸白：页面画布；
- 浅米与奶油色：分区、卡片和选中表面；
- 暖黑：主文字和 AI 工作面；
- 深陶土珊瑚：少量主操作和关键链接；
- 低饱和青绿：完成；
- 琥珀色：提醒和待处理；
- 暖灰：次级文字和边线。

最终色值必须通过正文 `4.5:1`、大字与控件边界 `3:1` 的对比度验收后冻结。间距以 `4 / 8 / 12 / 16 / 24 / 32 / 48 / 96` 为主阶；圆角以 `6 / 8 / 12 / 16` 为主。页面级卡片默认无阴影，弹层可以使用克制阴影。

正式版必须自托管经过授权核验的中文衬线和无衬线字体，并保留系统回退。不能依赖开发电脑已安装字体，也不能接受 Windows 11 意外回退到宋体后仍判定视觉一致。

## 四、日程页面正式结构

### 1. 默认视图

日程页不再把横向周表作为唯一入口。默认结构为：

```text
月份与切换
→ 月历概览（日期 + 少量状态点）
→ 本月或当日摘要
→ 当前选中日期与课程数
→ 按时间排序的纵向课程卡片
→ 新建课程
```

- 手机默认显示“月历概览 + 当日议程列表”。
- 桌面使用“月历概览 + 选中日议程”的双栏或上下组合，不能简单放大手机稿。
- 周视图可以作为教师查看冲突和整体负荷的次级视图，但不能强迫手机用户横向拖动周表才能看当天课程。
- 切换日期后，页面地址、浏览器前进后退和刷新必须保留所选日期。
- “今天”由可信服务端时间确定，不信任浏览器本机日期。

### 2. 月历概览

- 每格只显示日期、选中态和最多三枚概览标记，避免塞入课程名称。
- 标记可表达“有课、冲突、变更待确认”等不同事实，但必须有图例或可读文字，不能只靠颜色。
- 选中日期、今天和存在异常的日期必须能被清楚区分。
- 键盘可逐日移动并选择；读屏能读出日期、课程数和异常数。

### 3. 摘要区

摘要用于帮助教师快速判断工作量，不照抄参考图里的收入、时薪和课程数字。首选候选项为：

- 当日课程；
- 本月已完成课时；
- 待确认的改期或冲突；
- 待续费学生。

如果未来显示收入或单价，必须明确区分“计划金额、已登记缴费和已完成课时”，不能把预计收入显示为已到账。

### 4. 当日课程卡片

每张卡片至少包含：

- 开始和结束时间；
- 学生姓名；
- 课程或科目；
- 上课方式或地点；
- 当前状态；
- 与该课程直接相关的待确认事项。

可选信息包括课时消耗、已登记金额和备注，但不能让金额压过课程本身。卡片左侧色条只作辅助编码，状态仍需文字说明。

点击课程进入详情或安全操作面板；完成、取消、恢复、改期等高影响操作必须显示对象、变化和结果，不允许整卡误触直接执行。

### 5. 新建课程

- 手机可使用不遮挡内容的悬浮新增按钮，触控区域至少 `44 × 44px`。
- 桌面使用页面主按钮，不强制沿用悬浮按钮。
- 新建、改期和冲突检查应保留当前日期及学生上下文。
- 参考图里的“开始实时动态”不属于当前产品范围，不进入日程页。

## 五、AI 助手会话侧栏

### 1. 桌面端

- AI 页面左侧保留可展开的会话栏，展开时显示完整会话信息，收起后只保留会话入口和新建入口。
- 收起只改变可见宽度，不能卸载当前会话、清空输入草稿或打断正在生成的内容。
- 会话栏和聊天正文各自滚动；长会话列表不能带着输入区一起滚走。
- 当前会话必须有清楚的选中状态，不能只依靠一条很浅的色差。

### 2. 手机端

- 聊天页左上角提供明确的会话按钮，点击后从左侧展开覆盖式抽屉；正文保留在后方并降低视觉权重。
- 抽屉不能挤压聊天正文，也不能依赖横向滑动才能打开；点击遮罩、关闭按钮或系统返回均可关闭。
- 选择会话后抽屉关闭并显示目标会话；返回聊天时保留该会话尚未发送的输入草稿。
- 新建会话入口在抽屉顶部和聊天页顶部至少保留一处，触控区域不小于 `44 × 44px`。

### 3. 会话栏内容

从上到下保持以下顺序：

```text
新建会话
→ 搜索历史会话
→ 当前会话
→ 最近会话
→ 更早会话
```

每个会话条目优先显示：

- 简短标题；
- 明确的学生或工作上下文；
- 最近状态，例如“有待确认操作”“生成中”“上次失败”；
- 当前会话选中态。

不在列表中直接展示长段聊天内容或未遮蔽的敏感学生信息。已确认的是列表、选择、切换和展开方式；新建与搜索是推荐最小能力。重命名、置顶、归档、删除及其数据保留语义应在 W5 前单独冻结，不因参考产品存在而自动进入 V1。

### 4. 上下文与安全

- 每个会话拥有独立草稿、消息、学生上下文和待确认操作，切换时不能串到另一会话。
- 从学生详情进入 AI 时，可以显式带入该学生；从全局入口新建会话时不默认猜测学生。
- 待确认操作永远绑定原会话和原学生；教师切换会话后不能误确认另一个会话的操作。
- 如果 V1 支持删除会话，必须先定义删除对象和保留规则，并说明是否影响已经由教师确认写入的学生、日程、缴费或课堂记录。
- 会话地址可刷新、深链和前进后退恢复；加载失败不能悄悄回到另一会话。

### 5. 状态与可访问性

- 覆盖首次使用、无会话、搜索无结果、加载、生成中、部分完成、失败和离线状态；只有冻结删除能力后才增加“会话已删除”。
- 手机抽屉使用导航或对话框语义，打开后管理焦点，关闭后把焦点还给会话按钮；支持 `Esc` 和读屏状态播报。
- 展开与收起遵循减少动画偏好；动画不能阻止教师立即点击目标会话。
- 375、390、430px 手机宽度不能出现页面级横向溢出，输入区不能被抽屉或手机底部导航遮挡。

## 六、首批组件

- 页面壳、侧栏与手机导航；
- 月历、日期单元、状态图例；
- 当日议程列表、课程卡片、课程状态；
- 摘要卡；
- 主按钮、次按钮、危险按钮和图标按钮；
- 输入、选择、日期与时间控件；
- 空状态、加载、错误、部分成功和重试；
- 弹层、确认面板、Toast 和可访问状态播报；
- 暖黑 AI 建议卡、确认卡和执行结果卡。
- 会话侧栏、手机会话抽屉、会话条目、搜索、当前上下文标识和会话状态。

组件先形成独立规则，再进入页面；不允许各页面各自发明颜色、圆角和状态样式。

## 七、明确不能继承的内容

- 原型中的“发送给家长”按钮和不可撤回外发文案；正式版只能复制、导出或由教师标记为已人工发送。
- 教师自行录入模型供应商和 API Key 的演示设置。
- 浏览器内存数据、刷新回种、关键词假 AI、随机成功和本机时间。
- 把计划金额显示成实收、直接修改余额或完课后产生负课时。
- 点击 `div` 充当链接或按钮、无焦点管理的弹层、无状态播报的 Toast。
- 参考日程图中的高饱和粉色主题、实时动态按钮、固定五项底栏和第三方品牌元素。
- 豆包的品牌名称、蓝色主题、图标、文案或完整页面布局；只参考“菜单进入会话抽屉”的交互意图。

## 八、视觉验收 Gate

首批 Today、日程、Students、Student detail 和上下文 AI 页面必须同时提供 `1440×900`、`1366×768`、`390×844` 三档真实浏览器截图，由用户确认后再批量扩展。

日程页额外必须通过：

- 月份切换、选日、刷新、深链和前进后退；
- 有课、无课、冲突、取消、已完成、待确认和加载失败状态；
- 375、390、430px 手机宽度无页面级横向溢出；
- 周表若存在，只能在自己的容器内滚动；
- Windows 11 Edge/Chrome 的 125%、150% 系统缩放和 200% 浏览器缩放；
- 正文与小字对比度至少 `4.5:1`，控件边界和焦点至少 `3:1`；
- 所有操作可由键盘和读屏完成，手机操作目标至少 `44 × 44px`；
- 减少动画和 Windows 高对比度模式可用；
- 实际加载的是正式字体，不是未察觉的系统回退。

AI 助手页额外必须通过：

- 桌面会话栏展开、收起和刷新恢复；
- 手机抽屉打开、关闭、选中会话和系统返回；
- 已确认的列表、选择、切换、展开与收起，以及用户后来确认的最小会话管理动作；
- 每个会话独立保存输入草稿、学生上下文和待确认操作；
- 会话切换、生成中切换、失败重试和登录过期均不串会话；
- 若包含删除会话，删除范围与已确认业务记录的关系符合冻结后的产品规则；
- 键盘、焦点、读屏和 `44 × 44px` 触控目标通过。

## 九、审查证据

原型浏览器核对说明位于 [`evidence/ui-reference`](./evidence/ui-reference/README.md)。二进制截图只留在本机并被 Git 排除；它们证明参考原型的页面和响应式状态已被阅读，不代表正式版已经设计或实现。
