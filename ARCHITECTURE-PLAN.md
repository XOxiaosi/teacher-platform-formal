# 教师 AI 工作平台｜正式版架构计划

> 状态：拟定，待产品契约确认后冻结
>
> 目标：为 150 名个人教师建立可迁移到中国大陆云端、可审计、可恢复的正式版底座；不整包继承旧项目。

## 一、架构结论

正式版采用模块化单体，不在 V1 过早拆成微服务：

```text
教师网页 / 手机网页 ─┐
独立管理后台 ────────┼→ API → PostgreSQL
微信私聊入口 → 微信网关┘   ├→ 对象存储
                            └→ 持久化任务 → Worker
                                             ├→ OCR
                                             ├→ ASR
                                             └→ AI
```

运行边界：

- 教师网页与管理后台是两份独立前端产物、两套登录入口。
- API 先作为一个模块化应用部署，教师路由和管理员路由保持严格隔离。
- Worker 处理 OCR、ASR、导出、媒体删除和耗时聚合，长任务不阻塞网页请求。
- 微信网关只处理绑定、入站消息、媒体下载和当前对话回复，不拥有学生或课程业务。
- PostgreSQL 同时保存业务事实和持久化任务状态；V1 不引入 Kubernetes、RabbitMQ、向量数据库。

## 二、数据真相分层

### 1. 原始事件

- 网页输入和微信入站消息；
- 来源记录和媒体元数据；
- 只追加，不被识别结果或 AI 总结覆盖。

### 2. 处理结果

- 语音转写版本、图片识别版本；
- AI 提取候选、置信度、供应商和重试结果；
- 换模型、重试或人工修改均产生新版本。

### 3. 已确认业务事实

- 学生、课程、课堂记录、成绩、沟通、待办和课时流水；
- 有歧义的识别或推断先进入待审核，不能直接成为事实。

### 4. 审计与账本

- 重要修改记录、管理员访问记录、任务尝试记录；
- 缴费和课时采用追加流水，可从流水重新对账。

### 5. 用户视图

- 今日工作台、学生时间线、课时余额、家长反馈和运营统计；
- 都是可重建的视图，不反向改写来源材料。

原件删除后仍保留指纹、类型、大小、提取结果、处理版本、删除原因和回执、教师修订历史。

## 三、账号与教师隔离

- 使用一个 PostgreSQL 多租户数据库，不沿用“每位教师一套数据库”。
- `Tenant` 表示教师个人工作空间，V1 每个空间只有一名 owner。
- 所有业务记录带 `tenant_id`，跨表关系同时校验租户和记录 ID。
- 数据库行级策略与应用查询过滤双层隔离。
- 租户身份只从可信登录会话或微信绑定解析，客户端不能自行提交租户身份。
- 管理员使用独立账号、会话、限流和审计；默认内容可见范围由 P-002 冻结。

## 四、微信安全边界

正式版只提供四类能力：

- 绑定：扫码、暂停、解绑、重新绑定；
- 入站：接收教师私聊文字、语音和图片；
- 媒体下载：只下载当前教师入站消息包含的材料；
- 回复：只回复某一条已经存在的教师入站消息。

硬约束：

- 每个回复都关联入站消息 ID；
- 不提供通用通知或定时微信发送接口；
- 群聊、朋友圈、自动加好友和群发直接拒绝；
- 未绑定、暂停或解绑时不执行后续业务；
- 同一外部消息只执行一次；
- 微信故障不影响网页使用；
- 供应商必须经过 150 个独立绑定、长期在线和合规边界的真实验证后才能冻结。

## 五、OCR、ASR 与媒体生命周期

```text
上传或微信材料
→ 隔离区存储
→ 校验类型、大小、指纹与安全性
→ 持久化处理任务
→ Worker 调用真实 OCR / ASR
→ 保存版本化结果与可信度
→ 形成业务候选
→ 教师审核或规则确认
→ 按 P-001 删除原件
```

- 作业状态、尝试次数和错误都落库，进程重启后继续处理。
- 重复材料与重复任务不能造成重复业务记录。
- OCR 原文、版面结果和 AI 解释分开保存。
- ASR 原始转写与人工修订分版本保存。
- 多图按批次和顺序组织；其他学生姓名、手机号等无关内容在进入后续 AI 前最小化。
- 正式环境若仍使用占位识别，健康检查和发布 Gate 必须失败。

## 六、代码库建议

```text
apps/
  teacher-web/          新教师端
  admin-web/            独立管理后台
  api/                  HTTP API
  worker/               识别、导出、清理任务
  wechat-gateway/       微信连接与入站适配

packages/
  api-contracts/        可运行时校验的请求和返回契约
  domain/               学生、课程、课时、沟通等纯业务规则
  application/          用例、确认、审核和视图生成
  database/             数据模型、迁移和租户隔离
  media/                对象存储与媒体生命周期
  providers/            OCR、ASR 和 AI 适配器
  channel-wechat/       微信协议适配
  observability/        日志、指标和请求追踪
  testing/              合成数据与跨租户测试工具
  ops/                  备份、恢复、导出和迁移工具

infra/
  container/
  deploy/
  monitoring/

docs/
  product/
  contracts/
  decisions/
  runbooks/
  verification/
```

技术延续原则：继续使用 TypeScript、npm workspaces、React/Vite、Express、Prisma/PostgreSQL，减少无产品收益的框架切换；新增运行时请求/返回校验。所有生产源码执行 500 行上限。

## 七、云端编译与发布

- 生产目标为固定 Linux amd64、固定运行时和固定 PostgreSQL 大版本。
- macOS 和 Windows 只负责开发及浏览器验收。
- 干净 Linux 环境使用锁文件安装、测试和构建。
- 使用多阶段容器构建；API、Worker、微信网关共享同一镜像摘要，以不同命令运行。
- 教师端与后台分别生成带内容哈希的静态产物。
- 数据迁移使用同一发布镜像中的一次性作业，不在正式服务器临时构建或改代码。
- 预发布验证通过的同一镜像摘要提升到正式环境，不二次编译。
- 密钥、真实数据和供应商凭据不进入镜像。
- 每次候选版本生成文件清单、SHA-256、依赖清单和安全扫描证据。

## 八、旧项目处置原则

### 保留并移植

- 可信服务端时间、错误语义；
- 待确认操作、幂等执行、原子修改日志；
- 日程冲突和状态规则、学生识别规则、今日视图投影；
- 家长反馈证据门槛与快照；
- 媒体指纹、真实类型识别、加密接口和安全下载思想；
- 导出、备份恢复、优雅退出和 SSRF 防护的规则与证据测试。

### 提取规则后重写

- 数据模型与初始迁移；
- 教师与管理员身份、API 契约和路由装配；
- 全部教师前端、管理后台；
- 微信、OCR、ASR、任务队列和对象存储生命周期；
- 缴费与课时账本、周期课程；
- 云端部署、可观测性和正式版 Gate。

### 不迁入

- 旧教师端视觉、内存路由和旧 CSS；
- 每教师数据库、建库和批量迁库工具；
- 主动微信通知、PushRecord；
- Telegram、企业微信和天气适配器；
- Windows 启动器、RC 包和 `dist`；
- 占位 OCR/ASR、内存任务 Map、全局单 Bot Token；
- 旧迁移链直接重放；
- 由后台 HTTP 直接启动系统命令的运维方式。

旧数据若未来获准迁移，应通过“导出—校验—转换—导入—对账”的独立工具完成，正式版不直接连接旧数据库。
